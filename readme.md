Objective

The goal is to develop a Python-based ETL data pipeline that:
    -Extracts visit data from a MySQL database.
    -Transforms this data to count the number of visits for each doctor on a specific day.
    -Loads the results into a JSON file for easy access and further analysis.